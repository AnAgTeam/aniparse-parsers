{
	"name": "aniparse-parsers",
	"version": "0.1",
	"dependencies": [
		"boost-json",
		"boost-regex",
		"boost-system",
		"fmt",
		"tl-expected"
	],
	"features": {
		"curl-backend": {
			"description": "The curl HTTP backend. Off for platforms with a native backend (Apple/NSURLSession); CMake selects this feature from the ANIPARSE_CURL_BACKEND option, so curl is not installed at all when the backend is off.",
			"dependencies": [
				"curl"
			]
		}
	}
}
